/* Escreva um programa que leia um numero natural positivo N e gere a lista de todos os
elementos da lista A(n)= sigma(n),onde sigma(n) é a somatoria dos divisores de n com n <=N,
ex: para N=10, o programa vai imprimir :1,3,4,7,6,12,15,13,18. */
#include<stdio.h>

main(){
	
 int N,A[10],n,sm,sig=0;
 
 scanf("%d",&N);
 
 for(n=0;  n <=N; n++){
 	printf("%d",A[n]);
 
 }
 
 printf("%d\n",sm);
}
